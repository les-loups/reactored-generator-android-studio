package ${SCREEN_PACKAGE}

import ${REACTORED_PACKAGE}.base.BaseActivity

class ${ACTIVITY_NAME} : BaseActivity<${ACTION_NAME}, ${STATE_NAME}, ${FLASH_NAME}, ${VIEW_NAME}>() {

    override val reactor by lazy { ${REACTOR_NAME}(kodein) }

    override fun onCreateView(): ${VIEW_NAME} {
      return ${VIEW_NAME}(this)
    }
}