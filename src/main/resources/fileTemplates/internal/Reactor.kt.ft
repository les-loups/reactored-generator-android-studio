package ${SCREEN_PACKAGE}

import ${REACTORED_PACKAGE}.base.BaseReactor
import org.kodein.Kodein

class ${REACTOR_NAME}(kodein: Kodein) : BaseReactor<${ACTION_NAME}, ${STATE_NAME}, ${FLASH_NAME}>(${STATE_NAME}(), ${FLASH_NAME}())  {

    override fun dispatch(action: ${ACTION_NAME}) = when (action) {
        is ${ACTION_NAME}.FirstAction -> firstAction()
    }


    // Actions

    private fun firstAction() {

    }

}
