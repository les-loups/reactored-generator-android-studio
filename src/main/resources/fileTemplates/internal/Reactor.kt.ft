package ${SCREEN_PACKAGE};

import ${REACTORED_PACKAGE}.Reactor
import ${REACTORED_PACKAGE}.Store

class ${REACTOR_NAME} : Reactor<${ACTION_NAME}, ${STATE_NAME}, ${FLASH_NAME}> {

    override val store = Store<${STATE_NAME}>(${STATE_NAME}())

    override val flash = Store<${FLASH_NAME}>(${FLASH_NAME}())

    override fun dispatch(action: ${ACTION_NAME}) = when (action) {
        is ${ACTION_NAME}.FirstAction -> firstAction()
    }


    // Actions

    private fun firstAction() {

    }

}
