package ${SCREEN_PACKAGE}

data class ${STATE_NAME}(
    val to: String = "do"
) {

    enum class Navigation {
        None;

        var payload: Any? = null
    }

    data class ${FLASH_SUBCLASS_NAME}(val navigation: Navigation = Navigation.None)
}